# new image that starts from simple-web-service:alpine
FROM devopsdockeruh/simple-web-service:alpine
# when docker run is executed, we run the hello command
CMD server

# command to run the container is >> docker run web-server